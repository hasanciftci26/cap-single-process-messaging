entity Personnels {
    key personnelNo : Integer;
        firstName   : String(50);
        lastName    : String(50);
};

entity Employees {
    key personnelNo : Integer;
        firstName   : String(50);
        lastName    : String(50);
};
